{
  "name": "bc-fishing-r2",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare R2 Worker — serves data files + shard resolution API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "~5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
