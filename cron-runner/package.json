{
  "name": "bc-fishing-cron",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker on a Cron Trigger — dispatches GitHub Actions workflows punctually (replaces GHA's flaky schedule:)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0 || ^5.0.0",
    "typescript": "~5.7.0",
    "wrangler": "^4.0.0"
  }
}
