[pytest]
pythonpath = .
testpaths = regulation_mapping_v2/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (deselected by default, run with -m slow)
addopts = -m "not slow"
