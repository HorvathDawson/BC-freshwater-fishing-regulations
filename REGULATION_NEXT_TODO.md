- need to find municipal regulations outside of Lost Lagoon (Stanley Park): Fishing is banned. The lagoon is an ecologically sensitive area that suffers from poor water quality and shallow depths due to a lack of natural tidal flushing.  Beaver Lake (Stanley Park): Fishing is not permitted. It serves as a rare, naturally occurring salmon-bearing stream system within the city limits.  
  - considered an OSM `fishing=*` tag shortcut, then dropped it — BC's own regulations are already the authoritative source for where fishing is allowed/banned, so a second, OSM-community-maintained permission layer would just be a redundant (and potentially conflicting) source of truth. `fetch_overpass_fishing_access` was removed from `data/fetch_data.py`.
  - DONE: both wired as a new `source: "municipal"` regulation tier (`BaseRegulationDef.source` in `pipeline/enrichment/models.py` widened from `zone`/`provincial` to add `municipal`/`land_access`). Turns out neither needed OSM geocoding at all — both are real FWA lake polygons already, confirmed live against the source gpkg: `WATERBODY_KEY 329532481` (Lost Lagoon, GNIS_ID 18004) and `329532620` (Beaver Lake — specifically the Stanley Park one; there are 14 "Beaver Lake"s in BC, disambiguated by centroid). Matched via the existing (previously unused) `waterbody_keys` direct-match dispatch in `base_reg_assigner.py` — no admin polygon, no buffer/intersection risk. Confirmed live in the deployed reach shards post-build (`MUNICIPAL_LOST_LAGOON_CLOSURE` / `MUNICIPAL_BEAVER_LAKE_CLOSURE`).
  - DONE: rendered pinned at the top of the info panel in both apps (municipal closures sort to priority 0, same tier as any other "closed" reg) — webapp `InfoPanel.tsx` grouping/sort reworked; mobile `InfoPanel.tsx` got sort/priority logic added from scratch (previously just an unsorted flat list, no grouping concept at all).
- need to add protected areas (private access) like watersheds https://www.openstreetmap.org/relation/13480215#map=11/49.4420/-122.8718. this land ownership is improtant. 
  - DONE (data side): `land_access` fetch added in `data/fetch_data.py` (Overpass, scoped to the BC admin boundary, matches this exact Coquitlam Watershed example).
  - DONE (app side): wired into `pipeline/atlas/freshwater_atlas.py` (`_load_admin_layers`, mirrors the `aboriginal_lands` merge + BC-boundary-clip pattern) and `pipeline/tiles/tile_exporter.py`'s `_LAYER_MANIFEST`/`_layer_writers()`. New `AdminRecord.restriction_level` field (`"closed"`/`"restricted"`, derived from the OSM `access` tag — `no`/`private` → closed, `permissive`/`permit`/`destination`/`customers` → restricted) drives both the map's red hatch-pattern severity and a new `restriction_level_filter` on `AdminTargetDef`/`_assign_admin_targeted` so regulation text can target just the closed or restricted subset. Also added `landuse=military` to the Overpass query (was missing — DND land often carries no `access` tag at all).
  - DONE: `osm_admin_boundaries` retired and merged in, as anticipated below — its one hardcoded Malcolm Knapp Research Forest geocode query folded directly into `fetch_overpass_land_access()` as a supplemental named-area pass (same `ox.geocoder.geocode_to_gdf()` call, same OSM relation ID `1166294466`), so the `OSM_ADMIN` dataset type, `fetch_osm_roi_boundaries()`, and all `osm_admin` atlas/tile/regulation plumbing are gone — `land_access` is the single source of truth now. Malcolm Knapp's "no fishing" closure moved out of Zone Regulations into the new `source: "land_access"` tier (`land_access_knapp_forest_closure`, still `feature_id`-targeted so it stays scoped to just its own lakes) and now renders in a new "Land Use" info-panel section instead of "Zone Regulations."
  - DONE: two more generic, unscoped land_access advisories added (mirroring how the indigenous-lands advisory already applies province-wide with no `feature_id`) so every *other* restricted/private polygon (watersheds, DND, parking lots, etc — 14,859 polygons total after overlap-merging) surfaces something too, not just Malcolm Knapp. Live counts on the full province: 334 reg_sets picked up a "closed" advisory, 342 picked up "restricted."
  - DONE: `access=no` and `access=private` get distinct wording instead of one generic lumped message — split into `LAND_ACCESS_NO` ("no public access") / `LAND_ACCESS_PRIVATE` ("private property") / `LAND_ACCESS_CLOSED_OTHER` (covers ~109 `landuse=military` polygons with no explicit `access` tag at all, so they don't fall through uncovered), matched via a new `access_filter` on `AdminTargetDef`/`_assign_admin_targeted` alongside `restriction_level_filter`. New `AdminRecord.access` field carries the raw OSM tag through for this. Map hatch-severity styling is unaffected — still keyed on the coarser `restriction_level`, not this finer split.
  - DONE: land_access renders on the map (red fill + hatch overlay, reusing the existing colorblind-friendly `hatch-cross`/`hatch-diagonal` canvas patterns already used for parks_nat/eco_reserves — pattern density signals severity, not just hue) and, when a waterbody inside one is selected, the info panel shows the land-use notice pinned near the top (priority 1, right below municipal closures) — same build-time-baked `admin_targets` mechanism aboriginal_lands already used, nothing computed client-side on click.
  - DONE: the indigenous-territory advisory — previously its own separate provincial-tier group, sorted below synopsis regs — is now folded into the *same* "Land Use" group/header as land_access, at the same top priority, in both webapp and mobile.
  - real bug caught during the first full atlas rebuild: a genuinely invalid (self-intersecting) OSM polygon among the 14,859 land_access features crashed `geom.intersection(bc_boundary)` with a shapely `GEOSException` ("side location conflict"). Fixed with `buffer(0)` before the intersection — same defensive pattern already used elsewhere in `freshwater_atlas.py` — applied to both the new land_access loader and the pre-existing aboriginal_lands loader, which had the identical latent exposure and just hadn't hit it yet.
- current dawn/dusk calculations for each waterbody / current location. 
  - DONE: `suncalc` added to both `webapp` and `mobile`. New `useDawnDusk()` hook (near-identical port between the two apps) computes civil dawn/dusk for the current *map view center* (not the selected waterbody), recomputed only when the map pans past a 5km threshold or the calendar date changes — not on every pan tick, and with a coarse periodic check to still catch a date rollover during a long-idle session. Rendered as a line in the info panel between the header and the regulations list. Webapp wires it via a new `moveend` map listener; mobile via `MapLibreGL.MapView`'s `onRegionDidChange` (no such listener existed on either platform before).
- layer toggling like ihunter? 
  - partially there already: `tile_exporter.py`'s `_LAYER_MANIFEST` + per-layer MapLibre visibility flags are the plumbing. What's missing is the actual user-facing toggle panel — nothing currently reads `layer_manifest.json` to build a UI list the user can check/uncheck. (`_LAYER_MANIFEST` now also includes `land_access`, `forest_service_roads`, `water_access_points`, `waterfalls`, and no longer has `osm_admin` — still just plumbing, still no toggle UI.)
- bc forest servuce roads. 
  - source this from DataBC (`WHSE_FOREST_TENURE.FTEN_ROAD_SECTION_LINE`), not OSM — BC's backcountry road network (active/deactivated/gated status) is far more complete and authoritative there than OSM tagging.
  - DONE (data side): correct layer name is `WHSE_FOREST_TENURE.FTEN_ROAD_SECTION_LINES_SVW` (plural — the singular name doesn't exist), added as `forest_service_roads` reusing the existing `fetch_wfs_paginated()`/`WFS` mechanism. Confirmed live: has `LIFE_CYCLE_STATUS_CODE` (ACTIVE/RETIRED) — exactly the gated/deactivated signal we want.
  - DONE (app side): wired into the atlas (new `RoadRecord` model, new `_load_forest_service_roads()`) and tile pipeline (new `_write_roads()` writer). Renders solid when `LIFE_CYCLE_STATUS_CODE == "ACTIVE"`, dotted when `"RETIRED"`. A third status, `"PENDING"` (3,377 live), turned up that wasn't anticipated — falls through to the same solid default as active rather than being treated as deactivated. Name labels follow the line like stream labels do (webapp only so far — mobile's streams layer doesn't have its own line-label port yet either, pre-existing gap). Confirmed live on the full province: 284,669 road sections (196,370 active / 84,922 retired / 3,377 pending).
- new: water access points from OSM — the physical "can I actually get to this water" layer, distinct from land ownership or fishing permission. Broadened beyond just boat launches to cover every access-point icon type worth showing: boat launch (`leisure=slipway`), dock/pier (`man_made=pier`), marina (`leisure=marina`), designated fishing platform (`leisure=fishing`).
  - DONE (data side): single combined `water_access_points` fetch added in `data/fetch_data.py` (replaces the earlier narrower `boat_launches`), with a `poi_type` column so the frontend can pick a different icon per type. Live test: 16,700 features total — 15,710 piers, 740 boat launches, 208 marinas, 42 fishing platforms.
  - DONE (app side): wired into the atlas (new `PointRecord` model, `_load_point_layers()`) and tile pipeline (new `_write_points()` writer — handles way-derived non-point geometry, e.g. a pier mapped as an outline, by reducing to centroid). Webapp draws a distinct canvas glyph per `poi_type` (wedge = boat launch, dock-tie mark = pier, twin-mast mark = marina, hook = fishing platform) — shape-distinct, not just color-distinct, for colorblind accessibility — gated to zoom 13+. Mobile doesn't have an image/sprite registration path off its static `styleJSON` map style yet, so it renders the same data as colored circle markers instead (a documented parity gap, consistent with wetlands/hatch-pattern styling also not yet ported to mobile). Confirmed live on the full province: 16,704 features (15,712 piers, 742 boat launches, 208 marinas, 42 fishing platforms).
- new: BC parcel-level land ownership — `WHSE_CADASTRE.PMBC_PARCEL_FABRIC_POLY_SVW` (ParcelMap BC), free via DataBC WFS, has an `OWNER_TYPE` field (Private/Crown Provincial/Crown Agency/Federal/Local Government/First Nations/Untitled Provincial/Mixed Ownership/Unclassified). Not a licensing problem like it is for onX in the US — the real blocker was scale (2,490,357 parcels province-wide) — see below for how that's now handled.
  - DONE (data side): no longer gated, AND cut down for real (not just left at 2.49M and hoped for the best), AND switched to a completely different (better) fetch mechanism. Checked the actual `OWNER_TYPE`/`PARCEL_CLASS` breakdown live: 37% of "Private" parcels are `Building Strata` (individual condo units — no distinct ground footprint) and 46% of non-private parcels are `Road`/`Interest`/`Air Space` (right-of-way/easement registrations, not real land).
    - Switched from paginated WFS to the BC Data Catalogue's **bulk File Geodatabase download** (`pub.data.gov.bc.ca/.../pmbc_parcel_fabric_poly_svw.zip`, ~358MB, same download+extract pattern as `FWA_BC.zip` already used for the hydrography, just HTTPS instead of FTP) — one download instead of ~150+ paginated HTTP requests. `pyogrio`'s `where=` filters at read-time (pushed down to GDAL, so excluded rows are never even parsed).
    - `fetch_parcel_fabric()` keeps individual `Private` parcels (noise classes excluded) as `land_parcels_private` — 1,290,764, down from 2,044,965 — and dissolves every non-private `OWNER_TYPE` into one merged polygon each as `land_parcels_crown` — confirmed on the full province: 241,291 raw crown-type parcels → **8 polygons total** (one per ownership type: Crown Provincial, Crown Agency, Federal, Local Government, First Nations, Untitled Provincial, Mixed Ownership, Unclassified).
    - Ran the full thing end-to-end for real (not just a bounded sample): ~30 seconds total including the 342MB download. Treated like `streams`: included in every run by default, no `--layers` gate.
  - NOT DONE (app side): not wired into the atlas/tile pipeline yet — `land_parcels_private` is still ~1.29M rows, so tiling/rendering that one still needs its own plan; `land_parcels_crown` is only 8 polygons, small enough it could probably wire in directly whenever land_access/aboriginal_lands get wired in.
- new: waterfalls — `waterway=waterfall` in OSM (mostly nodes, some short ways), scoped to BC via Overpass.
  - DONE (data side): `waterfalls` fetch added in `data/fetch_data.py`, included in a full run. Live test found 2,411 features, most with `name`, some with `height` (metres).
  - DONE (app side): same `PointRecord`/`_write_points()` mechanism as water_access_points. Webapp gets its own canvas glyph (stacked cascading lines) plus an optional name label since waterfalls are namable landmarks; mobile gets a colored circle marker (same parity-gap note as water_access_points). Zoom-gated 13+. Confirmed live: 2,411 features.
- verified end-to-end on real production data (not just unit tests): full atlas rebuild (5,752,655 features across all layers, ~3 min), full PMTiles export (737MB, 21 layers, ~13 min via tippecanoe), and a full 5-phase enrichment build (2,245,671 reaches, ~40+ min — Phase 2/4's full-province spatial joins over 4.1M+ streams are the genuinely slow part, unrelated to anything new here) all ran clean. `pipeline/tests` (303 tests) and both frontends' `tsc --noEmit` pass throughout.
- reliability: `data/fetch_data.py`'s Overpass fetchers now share one `_overpass_query()` helper with retry-with-backoff (10s/20s, 3 attempts) on 429/502/503/504, after a real 504 hit `water_access_points` mid-run. Caught a real bug while testing this: `HTTPError` is a subclass of `URLError` in Python, so the first version of the transient-check treated *every* HTTP error (including permanent ones like a bad-query 400) as retryable — fixed to check `HTTPError.code` before falling through to the generic `URLError` branch.
- added `--missing-only` flag to `data/fetch_data.py` — skips layers already present in the output gpkg/files, so a partial/interrupted run can resume without re-fetching everything already done. Handles the multi-layer (`land_parcels_private`/`_crown`) and non-gpkg (file-based: `basemap`, `bathymetry_maps`, `osm_places`, `bathymetry_pdfs`) dataset types individually, not just a blind "layer name == gpkg layer name" check.
- new: **fish stocking records** from gofishbc.com (FFSBC), and a restructuring to house it.
  - Created `live-data/` — a new top-level directory for recurring, non-static data feeds (as opposed to `data/`'s mostly-static geospatial layers or `pipeline/`'s point-in-time regulation parsing). Moved `hydro-poc/` → `live-data/hydro/` (git mv, history preserved, same `python hydro_poc.py bootstrap|update` workflow).
  - Added `live-data/stocking/fetch_stocking.py` — reverse-engineered gofishbc.com's stocking-report tool: a `wp-json/stockingreports/v1/waterbodies/{region}/all` endpoint lists every stocked lake name per region (10 known regions), and `?reportType=lake&region=X&waterbody=Y` returns the full stocking history server-rendered directly in the page HTML (no API/JS needed, just GET + `html.parser`). Fetches **by name**, not `wbid` — confirmed live the report renders identically either way, so no wbid↔name resolution is needed just to fetch (cross-referencing records against our own waterbody data to attach a wbid is a separate later step). Caught and fixed a real bug during testing: the discovery endpoint double-JSON-encodes its response (a JSON string containing a JSON array as text) — first version only decoded once, so it iterated characters instead of names. Verified end-to-end live (Okanagan, 240 waterbodies discovered, real stocking records stored e.g. AGUR lake: 19 records with species/strain/genotype/quantity). Site sits behind a WAF that rate-limits bursts (hit a transient 403 during testing) — throttled + retry-with-backoff built in from the start given that.
  - Considered moving `pipeline/matching/in_season_scraper.py`/`in_season_resolver.py` into `live-data/` too (same "recurring data" category) — decided against `live-data/` specifically because, unlike hydro/stocking, this pair isn't a POC: checked and it's already fully in production (`.github/workflows/update-in-season.yml` runs it every 6 hours via `scripts/update-in-season.sh --upload`, pushing straight to Cloudflare R2; also called inline from `pipeline/enrichment/builder.py`'s main build). `live-data/` is for things still proving themselves out; this already graduated.
  - Did still move it, though — out of `pipeline/matching/` (whose real job is synopsis→FWA linking) into its own new `pipeline/recurring/` package, to make "runs on a schedule against a live site" visually distinct from the static-build-time matching logic. Updated every reference: `pipeline/enrichment/builder.py`'s two imports, both `python -m pipeline.matching.in_season_*` invocations in `scripts/update-in-season.sh`, the resolver's own `from .match_table import ...` (now `from pipeline.matching.match_table import ...`, a normal cross-package import), both modules' CLI docstrings, and `pipeline/matching/__init__.py`'s package docstring. Verified: fresh import of both moved modules succeeds, the old `pipeline.matching.in_season_scraper` path correctly 404s (nothing left behind), and the full `pipeline/tests` suite still passes (307 passed).
  - DONE: cross-referencing stocking records against FWA. Built `live-data/waterbody_db/` (since graduated, see below) — one shared db holding stocking (FIDQ) + gofishbc markers + bathymetry (WSA surveys) + a WDIC 1:50K reference cache, matched via a 6-stage chain (`match.py` → `match_fwa_gazette.py` → `match_fwa_identifier.py` → `match_fwa_override.py` → `match_wbid_gazette.py` → `match_final.py`). Confirmed live: **5,223/5,228 (99.9%) matched** to a real FWA `waterbody_key` across all three sources combined, the remaining 5 confirmed to have no current FWA polygon at all (not left unexplained). This fully superseded the standalone `stocking/`/`bathymetry/`/`common/` POCs, which were removed.
  - DONE: graduated `live-data/waterbody_db/` out of POC-land into `pipeline/recurring/waterbody_db/` (production). Converted its bare/script-style imports to proper package-relative ones (run via `python -m pipeline.recurring.waterbody_db.<module>` now, not `cd`-and-`python <file>.py`). Caught a real latent bug in the move: an override-table path was recomputed relative to the file's own location and silently resolved to a doubled `pipeline/pipeline/matching/overrides.json` (0 overrides loading instead of 29) — fixed by importing the canonical path from `pipeline.matching.match_table` instead of re-deriving it. Re-ran the full match chain post-move and confirmed identical results to pre-move.
  - DONE: **replaced `pipeline/matching/bathymetry_matcher.py` entirely** (deleted, along with its `bathymetry_overrides.json` and test) — it used to re-derive bathymetry→FWA matching from scratch (CSV + live gpkg spatial join) on *every* production build. In its place: `pipeline/recurring/waterbody_db/export.py` precomputes bathymetry PDF links + name variants + marker info once into `data/bathymetry_pdfs/waterbody_matches.json` (regenerated only when waterbody_db's own fetch+match reruns, same cadence as refreshing `data/wsa_bathymetry_maps.csv` — not tied to the regulations build), and a new `pipeline/enrichment/waterbody_accessor.py` just reads that JSON at build time (no gpkg dependency, no live matching). Cross-checked the new precomputed bathymetry map against the old matcher's live output before deleting it: 2,103/2,131 (98.7%) waterbody_keys overlap; sampled the non-overlapping cases on both sides and confirmed they're real, named lakes — the two independently-built engines make different tie-break judgment calls on genuine edge cases, not either being broken.
  - DONE: reaches now carry richer waterbody enrichment beyond just bathymetry PDFs — every known name variant from *both* stocking and bathymetry (not just one representative name per survey, as the old matcher did), tagged `source: "stocking"`/`"bathymetry"` in `name_variants`, plus a `marker` field with gofishbc "Where To Fish" amenity info (`more_info`, `description`, `photo_url`, `road_access`, `boat_launch`, `fishing_dock`, `campsite`, `washroom`, `wheelchair_access`, `hike_in_required` — a fixed whitelist, nothing else from the marker table). All deduped in one explicit final pass over the complete name-variant list (direct + admin + bathymetry + stocking together) so the same name can't appear twice regardless of which source(s) contributed it.
  - DONE: recurring stocking-info pipeline scaffolding — `pipeline/recurring/stocking_resolver.py` resolves FIDQ stocking records to reach IDs (simpler than in-season's name-based resolution, since stocking rows already carry a resolved `waterbody_key` — just a flat lookup against a new `poly_reaches.json`, written by `builder.py` right after the reach build). Wired inline into every full build (same non-fatal try/except pattern as the existing in-season block) so `stocking.json` always ships fresh. Confirmed live on a full build: **2,314/2,318 (99.8%) resolved to a real reach_id**. `scripts/update-stocking.sh` mirrors `update-in-season.sh`'s structure (minus the scrape step — stocking's "scrape" is just reading the already-fetched `waterbody_db.db`).
  - NOT DONE: no GitHub Actions workflow wired up for stocking yet (deliberately, this round) — nothing in the frontend consumes `stocking.json`'s actual content yet, so there's nothing to keep fresh on a schedule. `scripts/update-stocking.sh` exists and works standalone whenever that lands.
  - NOT DONE: stocking-info and marker-info **display** in the app (web/mobile) — the data pipeline produces `stocking.json` (per-waterbody release history) and attaches `marker` to reach shards, but nothing in `webapp`/`mobile` UI reads either yet. Both `NameVariant`/`NameVariantEntry` (extended with a `'stocking'` source) and a new `MarkerInfo` type are already defined across all four frontend type-duplicate locations (`mobile/src/types/regulations.ts`, `mobile/src/utils/featureUtils.ts`, `webapp/src/services/waterbodyDataService.ts`, `webapp/src/utils/featureUtils.ts`) ready for when display work starts.
  - NOT DONE: refreshing `waterbody_db.db` itself (re-running the fetch+match chain against live FIDQ/WSA/gofishbc data) and re-exporting `data/bathymetry_pdfs/waterbody_matches.json` is still a manual, occasional step — no automation around *that* cadence either.
