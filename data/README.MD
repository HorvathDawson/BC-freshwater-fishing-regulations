# Data Directory

## Getting the Data

To download the FWA (Freshwater Atlas) data from GeoBC:

```bash
wget -r --no-parent ftp://ftp.geobc.gov.bc.ca/sections/outgoing/bmgs/FWA_Public/
```

This command will recursively download all files from the FWA Public directory without ascending to parent directories.