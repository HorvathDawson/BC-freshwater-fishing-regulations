data/ftp.*
**/*.gdb
pycache/
*.pyc
*.pyo
__pycache__/
*.graphml
*.gpickle
*.pickle
*.gdb
*.gpkg
*.pmtiles
*.geojsonseq
# *.pdf
# Bulk-downloaded WSA bathymetry survey map PDFs (mirrored to R2, not committed).
# waterbody_matches.json is NOT one of these — it's the committed, periodically
# -refreshed precomputed matches artifact (see pipeline/recurring/waterbody_db/export.py).
data/bathymetry_pdfs/*.pdf
scripts/output/fwa_modules
scripts/output/synopsis_pipeline
scripts/output/fishing_synopsis.pdf
cache/
# Environment variables with API keys
.env
.env.local
.env.*.local
output/
**/node_modules/

# Vite build cache / dep pre-bundling
**/.vite/

# Python virtual environments
.venv/
venv/

# Wrangler local state (Miniflare R2, seed stamps)
**/.wrangler/
.seed-stamp
**/.seed-stamp

# macOS Finder metadata
.DS_Store
**/.DS_Store

# Local Claude Code tooling — not part of the project
.claude/
CLAUDE.md
.rtk/
graphify-out/